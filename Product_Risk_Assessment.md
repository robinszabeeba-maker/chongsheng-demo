# 面向产品经理的技术风险评估报告

> **评估依据**：[API_Documentation.md](./API_Documentation.md)、[mysql_scan_report.md](./mysql_scan_report.md)  
> **评估日期**：2026-06-11  
> **阅读对象**：产品经理、业务负责人  
> **说明**：本报告将文档与数据库扫描结论翻译为「对用户、功能、运营、迭代」的影响；不涉及改造方案设计。表中「字段未披露」表示扫描报告仅有表名，具体列需与技术确认。

---

### 一、用户隐私与数据安全风险（含合规）

| 风险定位 | 业务/用户影响 | 影响范围 | 严重程度 | 建议产品行动 |
|----------|---------------|----------|----------|--------------|
| 管理后台默认账号 `ADMIN_USERNAME` / `ADMIN_PASSWORD`（文档示例 `admin` / `suoweilai123456`） | 若生产仍用默认口令，外人可进入后台，查看/导出全部客户调用记录、重置客户额度、删除审计日志 | 全部 API 平台客户 | 高 | 立即与技术确认生产是否已改密；对外承诺数据安全前必须拿到书面确认 |
| 全部管理端接口（如 `GET /api/v1/logs/detail`、`POST /api/v1/keys`）仅单一口令保护 | 内部任一知道口令的人权限相同，无法区分「只能看报表」和「能删数据」；出事难以追责 | 全部后台操作 | 高 | 推动分角色权限；敏感操作（删日志、重置额度）需审批或双人确认 |
| `request_logs.request_body` / `request_logs.response_body`（文档定义）+ `GET /api/v1/logs/detail?request_id=` | 客户发给 AI 的完整对话、业务数据可能被长期保存，后台一键可读 | 所有走代理接口的终端用户 | 高 | 确认对用户协议/隐私政策是否已告知「对话可能被记录」；企业客户需单独说明 |
| `GET /api/v1/logs` / `GET /api/v1/logs/by-user/{user_id}` | 可按用户维度拉取全部历史调用，便于运营分析，也便于内部过度查看某一客户 | 指定 user_id 的客户 | 高 | 定义「谁可以看客户明细」规则；对外 B 端合同写清数据访问边界 |
| `POST /api/v1/chat/completions` Body.`messages[].content` | 用户输入经平台转发给外部 AI 厂商，内容在第三方侧也可见 | 全部 Chat 调用用户 | 中 | 对外材料写清「会调用第三方模型」；政企客户评估是否需私有化 |
| `ms-api-prod.customer_consultation_info`（2,183 条，客户咨询信息，字段未披露） | 若含姓名、电话、公司等，泄露将直接引发用户投诉和监管问询 | 提交过咨询的 API 平台用户 | 高 | 与技术确认存哪些字段、谁能看；必要时更新隐私条款 |
| `ms-api-prod.user_auth_info`（1,674 条，用户认证信息，字段未披露） | 若存登录凭证或 Token，泄露可导致账户被冒用 | 平台注册用户 | 高 | 要求技术说明是否加密存储；评估是否需通知用户改密 |
| `ms-api-prod.user`（2,505 条） | 用户基础信息与调用日志可关联，定位到具体个人 | 全部平台用户 | 中 | 数据最小化原则：非必要不采集；导出需审批 |
| `ms-api-prod.product_trial` / `product_trial_logs`（2.4 万+ / 3,800+ 条） | 试用 Key 若保管不当，可被未授权大量使用，且难追溯是谁泄露 | 试用客户 | 中 | 明确试用 Key 发放、回收流程；合同约束试用范围 |
| `ms-api-dev-03-24.user_auth_info` 等开发快照库（与生产同结构、千级认证数据） | 测试/快照环境若权限更松，相当于「备份了一份生产用户数据」在外围 | 可能含生产拷贝的用户 | 高 | 问技术：快照是否含真实用户；对外承诺「生产数据隔离」前必须核实 |
| `ms_ai.medical_record`（15 万+ 条病历，约 1.5 GB） | 宠物问诊/病历属高敏感健康信息，一旦泄露品牌与合规损失大 | 宠物 AI 问诊用户 | 高 | 若 API 平台与问诊业务数据相通，必须在对外宣传中界定；否则需技术证明数据隔离 |
| `ms_ai.session_record` / `ms_ai.report_content`（21 万+ / 28 万+ 条） | 对话与报告全文泄露，用户会认为「AI 问诊内容不被保护」 | 问诊/报告用户 | 高 | 与法务评估健康类数据告知义务；B 端合同加数据条款 |
| `ms_ai_international` / `ms-ai-en` / `ms-ai-c` 等多套问诊库 | 同一用户数据可能分散在不同库，对外说不清「数据存在哪里」 | 国内/海外用户 | 中 | 制作「业务线—数据存放说明」一页纸，销售/客服统一口径 |
| `ms-mairui.dicom_extraction`（医疗影像相关，293+ 条） | 合作医疗场景数据泄露影响合作方信任与监管 | 迈瑞合作相关用户 | 高 | 确认 API 开放平台是否触达该库；触达则纳入合规评审 |
| `cy-robot.ai_message` / `community_post` | 机器人 App 对话与社区帖子含用户 UGC，泄露引发社区信任危机 | 机器人 App 用户 | 中 | 社区与 API 产品分开评估隐私条款 |
| `ms-multi-recog.logs`（9,700+ 条，占该库绝大部分空间） | 多模态识别上传内容可能留在日志里 | 识别试用用户 | 中 | 确认对用户上传图片/文件的保留时长 |
| `fish-recg-jd.fish_detection_raw`（27 万+ 条原始检测数据） | 鱼缸/图像类原始数据可能间接识别用户家庭场景 | 京东合作鱼缸用户 | 中 | 确认是否在 API 平台链路中；合作合同数据条款对齐 |
| `iot.device_auth_code` / `pet_smart.app_credentials` | 设备与应用凭证泄露可导致他人控制设备或冒用 App | IoT/智能舱用户 | 中 | 确认与 API 平台是否同一运营主体；统一安全话术 |
| `api_endpoints.api_key`（文档定义）+ `POST /api/v1/endpoints` Body.`api_key` | 平台替客户保管的「上游 AI 账号密码」，后台被攻破则所有客户的上游账号连带失守 | 全部依赖平台转发的客户 | 高 | 对外 SLA 和安全白皮书需覆盖「上游密钥托管」；大客户提供专属 Key 方案 |
| `POST /api/v1/ai-providers/test-connection` Body.`api_key` | 运营测试连通性时传入的密钥，可能被误留记录 | 参与测试的运营/实施人员 | 中 | 规范测试流程；禁止用生产 Key 在文档示例里演示 |
| `GET /api/v1/keys/debug?id=` 返回密钥校验信息 | 调试接口若在生产开放，等于给攻击者多一条窃取客户 Key 的路径 | 全部 API Key | 高 | 要求生产关闭或仅限内网；写进上线检查清单 |
| `POST /api/v1/keys` 响应 `data.api_key`（仅创建时返回一次） | Key 创建界面若被录屏、日志缓存，客户 Key 永久泄露，需作废重发 | 新开通 Key 的客户 | 中 | 产品界面强调「只显示一次」；提供 Key 轮换能力并写进帮助中心 |
| `api_keys.allowed_ips`（文档定义，是否生效未说明） | 客户以为限制了 IP 才安全，实际若未校验，Key 被盗可在任意地点滥用 | 配置了 IP 限制的客户 | 高 | 与技术确认白名单是否真实生效；未生效则下架该卖点或标注「即将支持」 |
| `GET /api/v1/health` / `GET /api/v1/version`（无需登录） | 对外暴露服务名称与版本，便于针对性攻击 | 全体访问者 | 低 | 对外网关闭或隐藏详细信息 |
| 用户端 `/docs`、管理端 `/admin/docs`（接口说明页） | 完整接口地图对外可见，降低攻击门槛 | 全体 | 中 | 生产环境应对外关闭说明页 |
| `new-api.two_fa_backup_codes`（表已建、暂无数据） | 未来若上双重验证，备份码存不好会导致「加了锁但不更安全」 | 未来启用 2FA 的用户 | 低 | 功能上线前单独做安全评审 |

---

### 二、业务数据可靠性与扩展制约

| 风险定位 | 业务/用户影响 | 影响范围 | 严重程度 | 建议产品行动 |
|----------|---------------|----------|----------|--------------|
| 文档四表 `api_endpoints` / `api_pools` / `api_keys` / `request_logs` ↔ 生产库 `ms-api-prod` 实际表不一致 | **对外介绍的「API 聚合平台」能力，与线上真实存数据的结构对不上**；按文档验收的功能可能在生产不存在 | 全部基于该文档的销售、实施、验收 | 高 | 先与技术定「现行系统是哪套」；文档、销售彩页、合同附件统一口径 |
| 文档 `request_logs` ↔ `ms-api-prod.product_usage_record`（11.6 万条，占生产库 89%） | 用量统计、账单、报表若字段对不齐，会出现「客户看到的用量和后台对不上」 | 计费与运营报表 | 高 | 推动产出「报表字段对照表」；对客账单逻辑需书面确认 |
| 文档 `api_keys` ↔ `ms-api-prod.product_trial`（2.4 万条） | 试用 Key、正式 Key 可能混在不同逻辑里，运营难以统一管理生命周期 | 试用转付费全流程 | 中 | 梳理试用→正式的产品规则；确认额度在哪张表扣减 |
| `new-api` 库（25 张表仅 7 条数据）vs 文档描述的 MS-AI-Platform | 文档描述的新平台在数据层几乎空白，**不能当作已上线能力对外承诺** | 对外宣传「多池调度、端点管理」等 | 高 | 销售物料区分「已上线 / 规划中」；合同只写已验证能力 |
| `ms-api-prod` + 7 个 API 相关库（dev/快照/test/portal/早期版本） | 8 套相似环境并存，运营可能看到过期数据，或误把测试当生产决策 | 内部运营、数据分析 | 中 | 要求技术标注各环境用途；运营看板必须标明数据来源 |
| `ms-api-dev-03-24.product_usage_record`（9.2 万条）等快照库 | 2024 年快照仍保留大量调用记录，与生产重复，合规「数据存多久」说不清 | 历史数据治理 | 中 | 确认快照可否下线；更新数据保留政策 |
| `init_tables.sql` 与线上模型不一致（文档全局注意事项 #9） | 新环境搭建可能缺字段，**新功能上线周期被拖长**，或部分客户环境行为不一致 | 新客户部署、私有化交付 | 中 | 私有化项目验收前要求环境对齐检查 |
| `api_keys.allowed_pools` 默认「空=允许所有池」 | 给客户分了「只能用 A 池」，新建 B 池后可能被自动放开，**多租户隔离名存实亡** | 多客户共用平台 | 高 | 默认改为「必须显式授权」；大客户合同写清池隔离 |
| `POST /api/v1/chat/completions` Body.`pool_id` 默认 `1` | 客户配置的默认通道不生效，请求总走到 1 号池，**出现用错模型、用错报价、延迟异常** | 未手动传 pool_id 的集成客户 | 高 | 集成指南写清必须传参；推动修复默认值逻辑 |
| `api_pools.endpoint_ids`（文档，池内端点列表） | 池配置在后台改了，可能出现「显示有、实际调不到」的半失效状态 | 依赖池调度的客户 | 中 | 池变更后增加「生效验证」产品流程 |
| `DELETE /api/v1/endpoints` / `DELETE /api/v1/pools` | 后台删端点/池后，客户 Key 仍指向已删除资源，**大面积调用失败** | 使用该池的全部客户 | 高 | 删除前增加「影响客户清单」确认；产品定义软删除+缓冲期 |
| `ms-ai-international.pet_info` + `pet_info_copy1`（重复表） | 同一业务两份数据，报表人数/次数可能翻倍或遗漏 | 国际版问诊业务报表 | 中 | 确认哪张表为准；报表口径写进 BRD |
| `ms-mairui.report_content_test_copy1` 等测试/复制表在生产库旁 | 运营或报表若连错表，**对外展示测试数据** | 迈瑞合作报表 | 中 | 要求清理无关测试表；报表注明数据源 |
| 33 个业务库同次扫描（问诊、API、鱼缸、官网等） | 新产品立项时不清楚「数据该落哪个库」，迭代越加越乱 | 全公司新产品线 | 中 | 建立「业务—数据库归属」登记表，立项必经 |
| `ms_ai` + `ms_ai_dev` + `ms_ai_international` 三套问诊库数据量均大 | 国内/国际/开发环境边界模糊，**功能在 A 库验证通过，生产却在 B 库** | 问诊类功能迭代 | 中 | 每个需求明确「改哪个库、影响哪些端」 |
| `ms-api-prod.order_detail`（1 万+ 条，无注释） | 订单与 `product_usage_record` 用量如何对齐不清晰，**对账、退款依据不足** | 付费客户 | 中 | 与财务一起定义「用量—订单—发票」单一来源 |
| `ms-api-test.user_roles` 等权限表为空 | 文档后台无分权，与「未来要做企业子账号」类产品规划冲突 | 未来企业版 | 低 | 企业版 PRD 前先确认权限底座是否可复用 |

---

### 三、接口稳定性对业务操作的影响（含重复提交、校验缺失等）

| 风险定位 | 业务/用户影响 | 影响范围 | 严重程度 | 建议产品行动 |
|----------|---------------|----------|----------|--------------|
| `POST /api/v1/chat/completions`（文档：不具备幂等性） | 客户系统网络超时后自动重试，**同一问题被 AI 回答多次、额度扣多次**，引发对账纠纷 | 全部 Chat 集成客户 | 高 | 帮助中心写「请勿盲目重试」；推动支持请求唯一编号；设计客诉处理流程 |
| `POST /api/v1/v1/completions`（同上） | 同上，影响使用旧版补全接口的客户 | Completions 用户 | 高 | 同上 |
| `POST /api/v1/chat/completions` Body.`pool_id` + `api_keys.allowed_pools` | 客户以为 Key 只能访问指定池，实际请求可带其他 pool_id，**可能越权使用更高档资源** | 配置了池限制的客户 | 高 | 越权未拦截则暂停该卖点；合同写清池边界 |
| `POST /api/v1/chat/completions` Body.`strategy` 默认 `race` | 一次用户提问可能触发多路同时请求，**平台成本成倍、上游账单异常**，最终转嫁为定价压力或服务缩水 | 全部未显式指定策略的客户 | 高 | 默认策略改为「轮流」；对客说明「极速模式」单独计费 |
| Chat 与 Completions 对 `pool_id` 默认行为不一致（Chat 默认 1，Completions 走 Key 默认池） | 同一客户混用两个接口时行为不一致，**排查问题困难、体验随机** | 同时集成两类接口的客户 | 中 | 集成文档突出差异；长期推动行为统一 |
| `GET /api/v1/v1/models` 路径 + 内部字段版本不一致（文档 #816） | 客户用官方 SDK 拉模型列表，**看到的模型与实际能调用的不一致**，集成 POC 失败 | 新接入客户 | 高 | POC 检查清单增加「模型列表 vs 实际调用」；修复前销售慎承诺「完全兼容 OpenAI」 |
| `POST /api/v1/v1/completions` / `GET /api/v1/v1/models` 路径双 `v1` | 标准集成方式直接报错或 404，**客户第一天就无法跑通** | 使用 SDK 默认路径的客户 | 高 | 对外改为「需按文档改路径」或推动修复；合同 POC 条款绑定路径验收 |
| `GET /api/v1/v1/models` 读错池字段（`provider_ids` vs `endpoint_ids`） | 列表展示 GPT-4，实际 Key 只能调别的模型，**上线后大面积调用失败** | 依赖模型列表做 UI 的客户 | 高 | 客户 UI 不要仅信 models 接口；提供「已开通模型」专用查询 |
| `POST /api/v1/pools/{pool_id}/test`（文档：模拟结果，非真实请求） | 运营在后台点「池检测通过」，**实际客户仍在失败**，故障发现滞后 | 依赖后台巡检的运营 | 中 | 后台 UI 标注「模拟检测」；SLA 监控改看真实客户成功率 |
| `POST /api/v1/endpoints/{endpoint_id}/test` | 每次检测消耗真实上游额度，**运营频繁点击会烧掉采购的 AI 额度** | 运营成本 | 中 | 限制检测频率；大池变更才触发自动检测 |
| `DELETE /api/v1/logs/cleanup?days=`（默认 30 天，不可逆） | 误点或参数填错会**永久删除调用记录**，客诉时无证据、合规审计缺失 | 全部历史调用 | 高 | 产品加二次确认+影响条数预览；仅超管可操作 |
| `POST /api/v1/keys/reset-quota?id=` | 误重置会让已耗尽额度的客户**突然恢复可用或产生计费争议** | 指定 Key 客户 | 中 | 操作加确认；记录操作人 |
| `POST /api/v1/keys` / `POST /api/v1/endpoints` / `POST /api/v1/pools`（均不具备幂等性） | 实施脚本或前端连点可能**重复创建 Key/端点**，造成管理混乱与额外成本 | 实施与运营 | 中 | 创建页防重复提交；同名冲突提示优化 |
| `api_keys.allowed_models` / `allowed_ips`（文档有字段，调用侧是否校验未说明） | 产品宣传「可限制模型/IP」，若未生效则**构成虚假承诺** | 购买企业安全特性的客户 | 高 | 逐项与技术验收；网站文案与真实能力对齐 |
| `POST /api/v1/chat/completions` Body.`max_tokens` 默认 `10000` | 单次请求可消耗极大额度，恶意或配置错误客户**一次打满配额** | 高用量客户 | 中 | 按套餐设单次上限；超额友好提示 |
| `POST /api/v1/keys` 默认 `rate_limit_per_day=10000` | 盗 Key 后单日可发起海量请求，**一天内造成大额上游账单** | 全部 Key | 中 | 按客户等级设默认日限；异常流量告警对产品可见 |
| `ai_providers.py` 与 `api_endpoints.py` 重复注册同一路由（文档 #8） | 后台「改端点」有时生效有时不生效，**运营配置与客户实际体验不一致** | 后台配置人员 | 中 | 关键配置变更后强制走「客户侧验证」步骤 |
| `POST /api/v1/chat/completions` Body.`n` 默认 `1` | 若客户传 n>1，一次请求多份回答，**额度成倍消耗** | 传 n 参数的客户 | 低 | 文档说明 n 的计费影响 |
| `GET /api/v1/logs` Query.`success` 为字符串 `true`/`false` | 运营筛选「失败请求」时参数易填错，**看不到真实失败记录，误判系统健康** | 运营、客服 | 低 | 后台筛选做成下拉，不要手填 |
| `POST /api/v1/ai-providers/test-connection`（不具备幂等性） | 实施同学反复点测试，**浪费上游试用额度** | 实施阶段 | 低 | 测试按钮加冷却时间 |

---

### 四、系统性能对用户体验和业务增长的限制

| 风险定位 | 业务/用户影响 | 影响范围 | 严重程度 | 建议产品行动 |
|----------|---------------|----------|----------|--------------|
| `ms-api-prod.product_usage_record`（11.6 万条，占库 89%，持续增长） | 后台「调用记录」「用量报表」**越来越慢甚至超时**，运营无法及时响应客户问题 | 全部客户的运营支持 | 高 | 与技术定容量红线；报表改「T+1 汇总」对产品可接受则先改交互 |
| `GET /api/v1/logs` / `GET /api/v1/logs/statistics?hours=168` | 查一周统计时后台卡顿，**大客户复盘会议现场拉不出数据** | 运营、销售 | 中 | 大客户提供「周报自动邮件」替代现场查询 |
| `GET /api/v1/logs/recent?limit=500` | 单次拉太多记录，页面白屏或导出失败 | 运营后台 | 中 | 列表默认少条数+分页；导出改异步 |
| `POST /api/v1/chat/completions` Body.`strategy=race` | 并行多路请求放大延迟波动和失败率，用户感觉**「有时极快有时报错」** | 追求稳定的 B 端客户 | 中 | 稳定性优先的客户默认推荐非 race 策略 |
| `api_endpoints.max_retries` 默认 `3` | 上游短暂故障时单用户请求被放大多倍，**高峰期的失败雪崩** | 高峰时段全部用户 | 中 | SLA 文案区分「平台可用」与「模型可用」 |
| `POST /api/v1/chat/completions` + `stream=true` | 长连接多占服务资源，大促或集中上线时**整体变慢** | 流式场景用户 | 中 | 大客户接入前做压测；限流策略对产品透明 |
| `POST /api/v1/chat/completions` Body.`max_tokens=10000` | 单请求占用服务时间很长，**其他客户排队变久** | 同实例全部租户 | 中 | 套餐区分「长文本」能力；超限排队提示 |
| `ms_ai.medical_record`（15 万+ 条，单表 1.5 GB） | 若 API 链路关联问诊库，**问诊高峰可能影响 API 响应**（同实例情况下） | 跨业务用户 | 中 | 确认 API 与问诊是否资源隔离；否的话扩容预算前置 |
| `fish-recg-jd.fish_detection_raw`（27 万+ 条，1 GB+） | 鱼缸业务数据膨胀拖慢整体备份与恢复，**故障恢复时间变长** | 京东合作线、同实例其他业务 | 中 | 合作业务 SLA 单独评估；与客户沟通维护窗口 |
| `ms_ai.report_content`（28 万+ 条，350 MB） | 报告类功能查询慢，用户等报告久、客服投诉多 | 问诊报告用户 | 中 | 报告生成改「异步+通知」产品形态 |
| `pet_smart.operation_logs`（4.7 万条占该库 96%） | 智能舱相关操作日志堆积，**设备侧问题排查变慢** | 智能舱用户 | 低 | 与 IoT 产品线统一日志保留策略 |
| `ms-multi-recog.logs`（9,700+ 条，单行体积大） | 识别试用功能响应随数据增长变慢 | 多模态试用用户 | 低 | 试用数据定期清理策略产品化 |
| Redis 限流（文档提及，故障时行为未说明） | 若限流失效可能全体过载，若全拒则**所有客户突然不可用** | 全部 API 用户 | 高 | 问技术：限流挂了是「全停」还是「放开」；写入对外 SLA 免责边界 |
| 管理端与用户端分端口且 Docker 默认只启用户端（文档 #10） | 部署配错时**只有客户能调、运营后台打不开**，或反之 | 内部运营 | 中 | 上线 checklist 含「后台可登录验证」 |
| `GET /api/v1/health` 无限流（文档 #5） | 易被恶意刷接口占带宽，间接影响真实客户 | 全体 | 低 | 对外网健康检查走网关限频 |

---

### 五、外部依赖与系统整体稳定性风险

| 风险定位 | 业务/用户影响 | 影响范围 | 严重程度 | 建议产品行动 |
|----------|---------------|----------|----------|--------------|
| 扫描架构关系：`ms-api-prod` → `ms_ai` / `ms-fish` / `ms-api-portal`（推断，待技术证实） | API 平台若依赖问诊/鱼缸等兄弟系统，**对方故障会导致 API 客户连带不可用** | API 平台客户 | 高 | 要求技术画「客户一次调用经过哪些系统」；合同 SLA 写清依赖边界 |
| 文档描述上游多供应商（OpenAI / DeepSeek 等）经 `api_endpoints` | 某一上游宕机或涨价，**客户可用模型变少或成本骤升** | 全部转发类客户 | 中 | 产品准备「模型不可用」公告模板；商务谈备用供应商 |
| `api_pools.fallback_pool_id`（备用池） | 主池挂了能否自动切换，文档未描述对客户透明规则 | 配置备用池的客户 | 中 | 确认备用池切换是否自动；对客户说明 failover 行为 |
| `POST /api/v1/pools/{pool_id}/test` 为模拟 vs `POST .../endpoints/{id}/test` 为真实 | **池级健康状态不可信**，对外宣称高可用缺乏依据 | 高可用诉求客户 | 中 | 对外「可用性」改基于真实监控而非后台测试按钮 |
| `new-api` 几乎无数据 vs `ms-api-prod` 为实际生产 | 产品路线图若基于文档新平台，**可能与真实运营系统脱节** | 产品规划 | 高 | 季度规划前与技术对齐「唯一生产系统」 |
| `ms-api` 早期版 + `ms-api-portal` + `ms-api-prod` 三代并存 | 门户站点、早期版本若仍被访问，**用户看到的信息与 API 行为不一致** | 官网注册/门户用户 | 中 | 确认 portal 是否仍运营；下线则更新所有入口链接 |
| `cy-robot` → `ms_ai` 问诊能力（扫描架构图） | 机器人 App 与问诊库耦合，**问诊维护窗口影响机器人 AI 功能** | 机器人用户 | 中 | 跨产品发版需联合通知 |
| WordPress 库 `petturex-net` 与业务库同扫描范围 | 官网若被攻击，**可能波及同环境的 API 数据** | 官网访客 + API 客户 | 中 | 官网与核心 API 是否同机？是则推动隔离 |
| `POST /api/v1/chat/completions` 远程图片 URL 处理 | 客户传图经平台转存，图片服务异常时**多模态调用整段失败** | 视觉/多模态客户 | 中 | 多模态功能单独标注依赖与限制 |
| 外部 AI 厂商可见完整 `messages` 内容 | 上游政策变更或数据事件会**连带影响本平台声誉** | 全部 Chat 客户 | 中 | 客户合同传递「经第三方模型处理」；关注厂商公告 |
| 管理端端口文档 `8001` vs 默认 `8061` 不一致 | 实施文档写错端口，**客户私有化部署第一天后台进不去** | 私有化交付 | 中 | 交付手册单一来源；实施验收含后台登录 |
| 用户端代理前缀 `/ms-ai-platform/api/v1` vs 直连 `/api/v1` | 客户网关配置错前缀，**集成成功但生产 404** | 走网关的客户 | 中 | 集成文档给「两种部署」对照表 |
| `GET /api/v1/version` 暴露版本 | 已知漏洞针对性攻击，**突发大规模不可用** | 全体 | 低 | 对外隐藏详细版本 |
| 同步写 `request_logs`（文档业务流程） | 写记录失败是否导致客户请求也失败，文档未说明 | 全部调用 | 中 | 确认「记录失败仍返回 AI 结果」；避免双输 |
| `strategy=race` 失败请求仍可能计费（文档调度说明隐含） | 客户觉得「没得到答案却扣费」，**信任危机** | 使用 race 的客户 | 中 | 对客计费说明写清 race 规则；提供用量明细申诉入口 |

---

### 六、超出当前文档覆盖、必须与技术确认的评估盲区

| 盲区主题 | 对产品可能的潜在影响 | 为什么当前文档看不出来 | 建议向技术获取什么信息 |
|----------|----------------------|------------------------|------------------------|
| 各表具体存哪些字段、是否加密 | 无法判断隐私条款是否够、能否通过企业客户安全问卷 | 扫描只有表名和行数 | 核心表字段清单 + 是否脱敏/加密 |
| `product_usage_record` 是否存完整对话内容 | 若存，数据保留多久直接影响合规和用户信任 | 表名看不出内容 | 抽样一条记录结构；保留周期政策 |
| 生产环境实际跑的是文档这套系统还是 `ms-api-prod` 那套 | **销售承诺的功能可能根本没上线** | 文档与生产表结构矛盾 | 一句话：「客户现在用的是哪套？」 |
| 33 个库是否在同一台数据库、同一套密码 | 一出问题是否「全线业务停摆」 | 扫描无部署拓扑 | 架构简图：API 平台与问诊/鱼缸是否同机 |
| 开发/快照库是否含真实用户数据 | 测试环境泄露等于生产泄露，**无法用「只是测试」安抚用户** | 只见 dev 库行数接近 prod | 快照来源与是否脱敏 |
| API 调用是否会写入 `ms_ai.medical_record` 等问诊表 | 若会，API 客户实际上在使用健康数据基础设施，**合同范围需扩大** | 架构图为推断 | 一次客户调用的完整链路说明 |
| 实际每日调用量、峰值、增长趋势 | 不知道何时会「突然变慢/宕机」，**大促或大客户接入无预案** | 无监控数据 | 近 30 天调用量曲线 + 峰值时间 |
| 限流系统故障时平台行为 | 可能突然全体不可用或全体无限流，**SLA 无法写** | 文档只写「用 Redis 限流」 | 故障时是停服还是放开 |
| 管理后台是否暴露公网、是否仍用默认密码 | 决定是否需要立即暂停对外获客 | 文档只有 localhost 示例 | 生产后台访问方式 + 是否已改密 |
| 生产环境接口说明页 `/docs` 是否关闭 | 若开着，竞争对手和攻击者更容易下手 | 文档写了路径未写生产策略 | 外网能否访问 docs |
| 客户 Key 的 IP/模型限制是否真实生效 | 网站卖点是否属实，**虚假宣传风险** | 有字段无校验说明 | 每项限制的黑盒测试结果 |
| 备份与恢复要多久 | 故障后客户停服几小时，**SLA 赔偿依据** | 完全未提及 | RPO/RTO 与最近一次演练时间 |
| 日志/数据是否跨境存储或同步 | 海外客户签约障碍，**违反当地数据法规** | 只见多个国际库名 | 数据物理位置 + 跨境路径 |
| 与上游 AI 厂商的合同及数据条款 | 厂商停用或变更政策时**平台无缓冲** | 未提及 | 主要上游合同到期日与数据条款摘要 |
| 谁可以在后台看客户完整对话日志 | 内部治理与 B 端合同「数据访问」条款 | 有接口无权限说明 | 后台角色列表 + 谁能看 detail |
| 重复请求（网络重试）目前实际扣几次费 | 客诉「多扣费」时产品无法给明确答复 | 文档说无幂等但未写计费规则 | 计费去重规则一页纸 |
| 私有化/Docker 交付是否包含管理后台 | 客户买来发现**只能调 API、无法自助管控** | 文档说 Docker 默认只启用户端 | 标准交付物清单 |
| 历史 dev 快照库何时下线 | 数据越堆，未来合规检查越难通过 | 无生命周期政策 | 非生产库清理计划 |

---

**使用建议（给产品经理）**

1. **先关高严重度、且影响「对外承诺」的项**：文档与生产不一致、默认口令、模型列表不准、无幂等重复扣费、日志含全文等。  
2. **销售/合同**：在技术确认前，避免使用「完全兼容 OpenAI」「全链路企业级安全」「多池灵活调度」等全称表述。  
3. **本报告可作为与技术开评审会的议题清单**：按「风险定位」列逐项要答案，答案回来后再更新对外材料。

**报告版本**：v1.0 · 2026-06-11
