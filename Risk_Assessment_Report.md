# MS-AI-Platform 风险评估报告

> **报告编号**：RAR-2026-001  
> **报告版本**：v1.1  
> **编制日期**：2026-06-11  
> **评估类型**：API 平台风险评估（API 文档审查 + 数据库扫描）  
> **编制说明**：本报告**仅**依据 [API_Documentation.md](./API_Documentation.md) 与 [mysql_scan_report.md](./mysql_scan_report.md) 进行静态评估；不包含任何架构/设计方案。部分结论需结合运行时验证（见第 6 章待确认事项）。需求明确后再另行输出设计方案。

---

## 文档信息

| 项目 | 内容 |
|---|---|
| 评估对象 | API 开放平台（文档描述能力 + 数据库实际状态） |
| 依据材料 | [API_Documentation.md](./API_Documentation.md)、[mysql_scan_report.md](./mysql_scan_report.md) |
| 评估方法 | 文档一致性审查、数据库结构扫描、扫描报告中的架构关系推断 |
| 报告受众 | 管理层、产品、研发、运维、DBA、安全合规 |
| 明确排除 | 未纳入任何独立设计文档；本报告不输出架构改造方案 |

---

## 目录

1. [执行摘要](#1-执行摘要)
2. [评估范围与方法](#2-评估范围与方法)
3. [风险总览](#3-风险总览)
4. [风险登记册](#4-风险登记册)
5. [分项风险说明](#5-分项风险说明)
6. [待确认与不好判断的事项](#6-待确认与不好判断的事项)
7. [处置建议与优先级](#7-处置建议与优先级)
8. [附录](#8-附录)

---

## 1. 执行摘要

### 1.1 评估结论

本次评估共识别 **22 项** 风险，其中 **高风险 11 项**、**中风险 9 项**、**低风险 2 项**。

**总体结论：API 文档描述的 MS-AI-Platform 能力模型，与数据库扫描反映的生产现状（`ms-api-prod` 为主）存在明显脱节；在两者关系未厘清前，不宜将文档能力直接等同于线上能力。**

### 1.2 四类根本问题

| 编号 | 问题类型 | 说明 | 影响 |
|---|---|---|---|
| A | **文档与生产脱节** | 文档描述 `api_endpoints` / `api_pools` / `api_keys` / `request_logs`；生产 `ms-api-prod` 为 `product_usage_record` / `product_trial` / `user` 等；文档对应库 `new-api` 仅 7 行 | 按文档开发/销售可能落空 |
| B | **承诺与体验不一致** | OpenAI 半兼容、Key 路由默认值错误、模型列表不准 | 集成失败、客诉 |
| C | **成本与权限不可控** | 竞速默认、重试无幂等、`pool_id` 可能越权 | 成本放大、权限绕过 |
| D | **数据与运维基础薄弱** | 33 库同扫描范围、跨库关联推断、日志表膨胀、dev 快照库并存 | 合规不清、DBA 压力、安全风险 |

### 1.3 关键数据（数据库扫描）

| 指标 | 数值 | 来源 |
|---|---|---|
| 业务库总数 | 33 | mysql_scan_report 总览 |
| 数据总量（估） | 4,940 MB | mysql_scan_report 总览 |
| 生产 API 库 | `ms-api-prod` — 101.8 MB，171,137 行 | mysql_scan_report |
| 调用记录表 | `product_usage_record` — 115,972 行，占 prod 库 **89%** | mysql_scan_report |
| 文档对应库 | `new-api` — 25 表，仅 **7 行** | mysql_scan_report |
| 关联最大库 | `ms_ai` — 2,167 MB，含 `medical_record` 等 | mysql_scan_report |

### 1.4 建议立即行动（P0，基于现有证据）

1. **厘清文档适用范围**：明确 [API_Documentation.md](./API_Documentation.md) 描述的是哪套代码/环境，与 `ms-api-prod` / `new-api` 的对应关系  
2. **治理 `product_usage_record`**：归档/分区策略 + 容量告警（当前已占 prod 库 89%）  
3. **确认文档所述缺陷是否仍存在于目标环境**：OpenAI 路径、`pool_id` 默认值、`allowed_pools` 校验（见 U-19、U-20）  
4. **完成跨库数据流确认**：扫描报告推断 `ms-api-prod` → `ms_ai` / `ms-fish` 等，需代码或链路日志证实（见 U-10、U-11）

---

## 2. 评估范围与方法

### 2.1 评估范围

| 纳入 | 不纳入 |
|---|---|
| [API_Documentation.md](./API_Documentation.md) 用户端/管理端 API 及全局注意事项 | 生产运行代码（未提供仓库访问） |
| [mysql_scan_report.md](./mysql_scan_report.md) 33 库扫描结果及架构关系推断 | 运行时流量、QPS、错误率 |
| 两文档之间的表模型/能力对照 | 渗透测试、字段级 DDL（扫描无字段详情） |
| | **任何架构/多租户/全球化设计方案** |

### 2.2 评估方法

```text
API 文档审查 ──→ 接口设计、认证、路由、日志、文档自述缺陷
        │
数据库扫描   ──→ 生产库表结构、数据量、dev 库并存、跨库关系推断
        │
对照分析     ──→ 文档表模型 vs ms-api-prod / new-api 实际表
        │
风险评级     ──→ 高 / 中 / 低
        │
待确认清单   ──→ 证据不足项单独列出，不做过度推断
```

### 2.3 风险等级定义

| 等级 | 定义 | 典型后果 |
|---|---|---|
| **高** | 可能导致集成失败、数据泄露、合规事故或生产大面积不可用 | 客户流失、监管风险、全站影响 |
| **中** | 影响体验、成本、运维效率或局部合规 | 客诉、对账纠纷、交付延期 |
| **低** | 可通过文档/流程缓解，影响面有限 | 实施困惑、误操作 |

---

## 3. 风险总览

### 3.1 按等级分布

| 风险等级 | 数量 | 占比 |
|---|---|---|
| 高 | 11 | 50% |
| 中 | 9 | 41% |
| 低 | 2 | 9% |
| **合计** | **22** | 100% |

### 3.2 按来源分布

| 来源 | 风险 ID | 数量 |
|---|---|---|
| 仅 API 文档 | R-01 ~ R-13 | 13 |
| 仅数据库扫描 | R-15, R-16, R-18, R-20, R-22 | 5 |
| 文档 + 扫描交叉 | R-14, R-17, R-19, R-21 | 4 |

### 3.3 按影响维度分布

| 影响维度 | 涉及风险数 | 代表风险 |
|---|---|---|
| 文档/生产一致性 | 8 | R-14、R-09 |
| 客户集成 / API 体验 | 8 | R-01、R-21 |
| 安全 / 合规 / 数据 | 7 | R-17、R-18 |
| 成本 / 计费 | 6 | R-03、R-11 |
| 运维 / 交付 | 5 | R-16、R-15 |

---

## 4. 风险登记册

| ID | 风险名称 | 等级 | 主要来源 | 建议优先级 |
|---|---|---|---|---|
| R-01 | OpenAI 兼容路径不一致 | 高 | API 文档 | P0 |
| R-02 | 调度默认值与 Key 配置不一致 | 高 | API 文档 | P0 |
| R-03 | race 竞速策略导致成本放大 | 高 | API 文档 | P1 |
| R-04 | 管理端认证薄弱、无 RBAC | 高 | API 文档 | P1 |
| R-05 | 客户端可 override pool_id 越权 | 高 | API 文档 | P0 |
| R-06 | 模型列表字段版本不一致 | 中 | API 文档 | P1 |
| R-07 | API Key 一次性下发不可找回 | 中 | API 文档 | P2 |
| R-08 | 请求日志可能存完整 Prompt | 中 | API 文档 | P2 |
| R-09 | 代码路由重复 + Schema 与生产库不一致 | 高 | API 文档 + 扫描 | P1 |
| R-10 | 池测试为模拟结果 | 中 | API 文档 | P2 |
| R-11 | 非幂等 + 重试导致重复计费 | 中 | API 文档 | P2 |
| R-12 | 部署/端口文档混乱 | 低 | API 文档 | P3 |
| R-13 | 日志清理接口无安全防护 | 低 | API 文档 | P3 |
| R-14 | **文档对象与生产库严重脱节** | 高 | API 文档 + 扫描 | **P0** |
| R-15 | **多套 dev/快照库与生产并存** | 高 | 扫描 | P1 |
| R-16 | **product_usage_record 日志膨胀** | 高 | 扫描 | **P0** |
| R-17 | **跨库关联推断、数据边界不清** | 高 | 扫描 | P1 |
| R-18 | **33 库同扫描范围、爆炸半径不明** | 高 | 扫描 | P1 |
| R-19 | 多区域/多版本 AI 库并存 | 中 | 扫描 | P2 |
| R-20 | 测试/复制表残留在业务库 | 中 | 扫描 | P2 |
| R-21 | new-api 库几乎无数据 | 中 | 扫描 | P2 |
| R-22 | API 库含用户认证与咨询 PII 表 | 中 | 扫描 | P2 |

---

## 5. 分项风险说明

### 5.1 A 类：文档与生产脱节（API 文档 ↔ mysql_scan_report）

#### R-14 文档对象与生产库严重脱节 【高 · P0】

| 项目 | 内容 |
|---|---|
| **风险描述** | [API_Documentation.md](./API_Documentation.md) 描述 MS-AI-Platform，数据模型含 `api_endpoints`、`api_pools`、`api_keys`、`request_logs`；扫描显示生产 API 库 `ms-api-prod` 核心表为 `product_usage_record`、`product_trial`、`user` 等，结构完全不同；文档可能对应的 `new-api` 库 25 表但仅 7 行。 |
| **证据** | API 文档附录数据模型；mysql_scan_report `ms-api-prod`、`new-api` 章节 |
| **可能影响** | 按文档对接/验收的能力在生产数据层不存在；研发与运维对「现行系统」认知分裂 |
| **建议（事实层面）** | 在文档首部标注适用范围；产出「文档能力 ↔ 生产库表」对照表；明确 `new-api` 与 `ms-api-prod` 各自状态 |

#### R-09 代码路由重复 + Schema 与生产库不一致 【高 · P1】

| 项目 | 内容 |
|---|---|
| **风险描述** | 文档全局注意事项 #8：`ai_providers.py` 与 `api_endpoints.py` 重复注册 `/endpoints` 路由；#9：`init_tables.sql` 与 ORM 字段不一致；扫描进一步证实生产库表与文档模型不匹配。 |
| **证据** | API 文档全局注意事项 #8、#9 |
| **建议** | 确认文档对应代码库中的路由注册顺序；校准 init SQL 与 ORM；**若文档代码即目标系统**，需说明与 `ms-api-prod` 的迁移/并行关系 |

#### R-21 new-api 库几乎无数据 【中 · P2】

| 项目 | 内容 |
|---|---|
| **风险描述** | `new-api` 分类为「AI 聚合平台」，25 表但总行数约 7（`users` 1 行，`tokens` 0 行等），数据量 0.39 MB。 |
| **证据** | mysql_scan_report `new-api` 章节 |
| **可能影响** | 若对外宣传文档中的 endpoints/pools 能力，与 `new-api` 数据层现状不符 |
| **建议** | 确认 `new-api` 是未上线、测试库还是与文档无关的遗留库；对外材料与实际情况对齐 |

---

### 5.2 B 类：API 文档自述的实现与体验风险

#### R-01 OpenAI 兼容路径不一致 【高 · P0】

| 项目 | 内容 |
|---|---|
| **风险描述** | `chat/completions` 为 `/api/v1/chat/completions`；`completions`、`models` 为 `/api/v1/v1/completions`、`/api/v1/v1/models`，与 OpenAI SDK 默认路径拼接不一致。 |
| **证据** | API 文档全局注意事项 #11 |
| **建议** | 在目标环境验证路径；若缺陷仍存在，统一路径并保留旧路径兼容 |

#### R-02 调度默认值与 Key 配置不一致 【高 · P0】

| 项目 | 内容 |
|---|---|
| **风险描述** | Chat 未传 `pool_id`/`strategy` 时，schema 默认 `pool_id=1`、`strategy=race`，而非 API Key 的 `default_pool_id` 和池默认策略。 |
| **证据** | API 文档全局注意事项 #4、#5；Chat Completions 注意事项 |
| **建议** | 确认生产/目标环境是否已修复；明确默认值优先级：Key > 池 > 全局 |

#### R-05 客户端可 override pool_id 【高 · P0】

| 项目 | 内容 |
|---|---|
| **风险描述** | API Key 配置含 `allowed_pools`，但请求体允许传 `pool_id`；文档未说明已强制校验。 |
| **证据** | API 文档 Key 配置与 Chat 请求体 |
| **建议** | 确认是否已实现 `pool_id ∈ allowed_pools`；未实现则视为安全缺陷 |

#### R-06 模型列表字段版本不一致 【中 · P1】

| 项目 | 内容 |
|---|---|
| **风险描述** | `list_models` 读取 `key_info.get("pool_id")` 和池的 `provider_ids`，而实际模型关联使用 `default_pool_id` 和 `endpoint_ids`。 |
| **证据** | API 文档 Models 注意事项 |
| **建议** | 统一查询逻辑，列表只返回真实可调用模型 |

#### R-03 race 竞速导致成本放大 【高 · P1】

| 项目 | 内容 |
|---|---|
| **风险描述** | 竞速策略并发请求多路上游；Chat schema 默认 `strategy=race`。 |
| **证据** | API 文档全局注意事项 #5、调度策略说明 |
| **建议** | 确认默认策略；评估 race 的成本记录与对账方式 |

#### R-04 管理端认证薄弱 【高 · P1】

| 项目 | 内容 |
|---|---|
| **风险描述** | 管理端全站 Basic Auth；文档示例含固定用户名密码；无 RBAC、MFA、操作审计描述。 |
| **证据** | API 文档认证方式章节 |
| **建议** | 确认生产是否仍使用文档示例凭证；评估管理端访问控制强度 |

#### R-07 ~ R-13 其他文档风险

| ID | 等级 | 描述 | 证据 |
|---|---|---|---|
| R-07 | 中 | Key 创建后明文仅返回一次，丢失需重建 | 全局注意事项 #6 |
| R-08 | 中 | 文档设计 `request_logs` 含 request_body/response_body，存在 Prompt 落库可能 | 日志管理章节 |
| R-10 | 中 | 池测试接口返回模拟结果，非真实上游拨测 | 池测试 API 注意事项 |
| R-11 | 中 | AI 请求无幂等键，超时重试可能重复计费 | Chat 注意事项 |
| R-12 | 低 | 管理端端口 8061 vs 8001 混用；Docker 默认只启用户端 | 全局注意事项 #10 |
| R-13 | 低 | 日志清理为物理 DELETE，无二次确认 | 全局注意事项 #7 |

---

### 5.3 C 类：数据库扫描发现的风险

#### R-16 product_usage_record 日志膨胀 【高 · P0】

| 项目 | 内容 |
|---|---|
| **风险描述** | `ms-api-prod.product_usage_record`：115,972 行，90.61 MB，占该库 **89%**；注释为「产品使用统计表」。 |
| **证据** | mysql_scan_report `ms-api-prod` Top 表 |
| **可能影响** | 持续增长导致查询变慢、备份窗口拉长、磁盘成本上升 |
| **建议** | DBA 评估归档/分区；配置表大小告警 |

#### R-15 多套 dev/快照库与生产并存 【高 · P1】

| 项目 | 内容 |
|---|---|
| **风险描述** | API 开放平台类共 8 库：`ms-api-prod` + 3 个 dev 快照（`ms-api-dev-03-24` 128,910 行、`ms-api-dev-3-20` 126,419 行、`ms-api-dev-09` 65,205 行）+ dev/test/portal 等，合计 343 MB。 |
| **证据** | mysql_scan_report 业务分类、各 dev 库详情 |
| **可能影响** | 快照库行量级接近 prod，可能含敏感或生产级数据；环境边界不清 |
| **建议** | 确认各 dev 库用途、数据来源、访问权限；非必要库下线或脱敏 |

#### R-18 33 库同扫描范围、爆炸半径不明 【高 · P1】

| 项目 | 内容 |
|---|---|
| **风险描述** | 单次扫描覆盖 33 库、约 4.9 GB，含 API 网关、AI 问诊（含病历）、鱼缸识别、IoT、WordPress、机器人 App 等；扫描报告**未说明**是否同一 MySQL 实例/主机/账号。 |
| **证据** | mysql_scan_report 总览、业务分类 |
| **可能影响** | 若同实例同高权限账号，单点泄露影响面大 |
| **建议** | 确认实例拓扑与账号权限（见 U-12、U-13）；按业务域评估隔离必要性 |

#### R-17 跨库关联推断、数据边界不清 【高 · P1】

| 项目 | 内容 |
|---|---|
| **风险描述** | 扫描报告「架构关系」推断：`ms-api-prod` 关联 `ms_ai`/`ms_ai_dev`（宠物 AI 问诊）、`ms-fish`/`fish-recg-jd`（智养鱼缸）、`ms-api-portal`；`ms_ai` 最大表 `medical_record` 约 1,556 MB。**此为扫描工具推断，未经代码或链路证实。** |
| **证据** | mysql_scan_report 第三节架构关系、`ms_ai` Top 表 |
| **可能影响** | API 调用数据是否流入问诊/病历库未知；合规边界无法陈述 |
| **建议** | 通过代码或一次全链路追踪确认实际调用关系；绘制已证实的数据流图 |

#### R-19 多区域/多版本 AI 库并存 【中 · P2】

| 项目 | 内容 |
|---|---|
| **风险描述** | AI 问诊类 9 库：`ms_ai`（2,167 MB）、`ms_ai_international`（376 MB）、`ms-ai-en`、`ms-ai-c`、`ms-ai-international` 等；扫描**无**统一用户/租户标识字段信息。 |
| **证据** | mysql_scan_report 业务分类、Top 10 |
| **可能影响** | 同一业务多库并行，数据归属与版本边界需人工梳理 |
| **建议** | 建立「库 → 业务线/环境/区域」说明表；确认与 `ms-api-prod.user` 是否有关联 |

#### R-20 测试/复制表残留在业务库 【中 · P2】

| 项目 | 内容 |
|---|---|
| **风险描述** | 如 `ms-mairui.report_content_test_copy1`、`ms-human.report_content_test` 等测试/复制表存在于业务库中。 |
| **证据** | mysql_scan_report 各库详情 |
| **建议** | 盘点并清理非生产必要表 |

#### R-22 API 库含用户认证与咨询 PII 表 【中 · P2】

| 项目 | 内容 |
|---|---|
| **风险描述** | `ms-api-prod` 含 `user_auth_info`（1,674 行）、`customer_consultation_info`（2,183 行，客户咨询信息）、`user`（2,505 行）；扫描无字段级详情，无法判断加密/脱敏状态。 |
| **证据** | mysql_scan_report `ms-api-prod` Top 表 |
| **建议** | 补充字段审查；确认存储内容与保留策略 |

#### R-08 补充（文档 + 生产对照）

| 项目 | 内容 |
|---|---|
| **风险描述** | 文档 `request_logs` 设计含完整请求/响应体；生产对应表推测为 `product_usage_record`，但**是否存 Prompt 无法从扫描确认**（见 U-05）。 |
| **证据** | API 文档日志章节；mysql_scan_report `product_usage_record` 行数/大小 |

---

## 6. 待确认与不好判断的事项

> 以下事项因缺少运行时证据、字段级 DDL 或代码访问，**无法从两份材料 alone 下定论**。确认后请更新本报告 v1.2。

### 6.1 系统边界（影响 R-14、R-21）

| # | 待确认问题 | 为何不好判断 | 建议确认方式 |
|---|---|---|---|
| U-01 | 生产 API 流量实际对应哪套应用？`ms-api-prod` 还是文档中的 MS-AI-Platform？ | 扫描只见库，不见部署 | 查网关/Nginx/进程与 DSN 配置 |
| U-02 | [API_Documentation.md](./API_Documentation.md) 与 `ms-api-prod` 是否同一代码仓库？ | 表结构完全不同 | 对比 prod 应用 git 与文档生成来源 |
| U-03 | `new-api` 库是未上线、测试环境还是与文档无关？ | 仅 7 行，含义不明 | 问负责人 + 查部署记录 |
| U-04 | 文档描述的 FastAPI 服务（8000/8061）是否在任意环境运行？ | 无运行态材料 | 端口与服务探测 |

### 6.2 数据内容（影响 R-08、R-16、R-22）

| # | 待确认问题 | 为何不好判断 | 建议确认方式 |
|---|---|---|---|
| U-05 | `product_usage_record` 是否存完整 Prompt/响应？ | 扫描无字段 DDL | `DESCRIBE` + 抽样 |
| U-06 | `product_usage_record` 与文档 `request_logs` 是否同业务不同世代？ | 表名/结构均未对照 | 字段对比 + 问历史负责人 |
| U-07 | `user_auth_info` 是否含明文凭证？ | 仅知行数 | 表结构 + 加密方案 |
| U-08 | `customer_consultation_info` 含哪些 PII？ | 仅知行数与注释 | 字段审查 |
| U-09 | dev 快照库是否含真实生产数据？ | 行数接近 prod | 抽样 user_id 对比 + 查拷贝来源 |

### 6.3 架构与跨库（影响 R-17、R-18、R-19）

| # | 待确认问题 | 为何不好判断 | 建议确认方式 |
|---|---|---|---|
| U-10 | `ms-api-prod` → `ms_ai`/`ms-fish` 是运行时调用还是业务并列？ | 扫描报告为推断图 | 查 API 网关下游调用代码 |
| U-11 | API 调用是否会写入 `ms_ai.medical_record`？ | 跨库关系未证实 | 全链路日志追踪 |
| U-12 | 33 库是否同一 MySQL 实例/主机？ | 扫描未说明拓扑 | DSN、`SHOW VARIABLES`、网络拓扑 |
| U-13 | prod 与 dev 库是否共用数据库账号？ | 影响 R-15、R-18 实际严重度 | 查各环境连接串权限 |
| U-14 | `ms_ai_international` 等与 `ms-api-prod.user` 是否同一用户体系？ | 多库并行，无统一 ID 信息 | 授权下做 ID 交叉比对 |

### 6.4 商业与合规（影响对外陈述边界）

| # | 待确认问题 | 为何不好判断 | 建议确认方式 |
|---|---|---|---|
| U-15 | 当前 API 平台实际服务哪些客户/产品线？ | 扫描无商业上下文 | 产品/销售/CS 确认 |
| U-16 | 是否已对客户提供数据存储/日志相关承诺？ | 合同材料未纳入评估 | 法务审查 SLA/DPA |
| U-17 | 国际库（`ms_ai_international` 等）与 API 平台是否同一对外产品？ | 仅能从库名推断 | 产品架构说明 |
| U-18 | 库之间是否存在定时同步/CDC？ | 扫描无任务信息 | 查 DTS/cron/同步脚本 |

### 6.5 文档所述缺陷是否仍生效（影响 R-01 ~ R-11）

| # | 待确认问题 | 为何不好判断 | 建议确认方式 |
|---|---|---|---|
| U-19 | `pool_id=1` 默认值问题是否已修复？ | 文档写「当前」行为 | 查目标分支 schema |
| U-20 | `allowed_pools` 校验是否已实现？ | 文档未标注已修复 | 代码搜索 + 接口测试 |
| U-21 | 生产环境默认调度策略是否为 race？ | 文档默认 race，prod 配置未知 | 查典型 Key/池配置 |
| U-22 | 管理端 Basic Auth 是否仍用文档示例密码？ | 文档含 `suoweilai123456` 示例 | 生产配置审查 |

### 6.6 汇总

| 类别 | 数量 | 说明 |
|---|---|---|
| 系统边界 | 4 | 决定「文档能不能代表生产」 |
| 数据内容 | 5 | 决定合规与日志风险等级 |
| 架构跨库 | 5 | 决定 R-17 能否关闭或升级 |
| 商业合规 | 4 | 决定对外怎么说 |
| 代码缺陷 | 4 | 决定 P0 API 项是否已完成 |

**说明：** 上述待确认项澄清后，再讨论是否需要新的架构/多租户/全球化方案；**本报告不包含设计方案。**

---

## 7. 处置建议与优先级

> 以下仅为**基于现有证据的处置方向**，非架构设计。具体方案待需求明确后另行输出。

### 7.1 优先级路线图

| 优先级 | 风险 ID | 行动项 | 负责方 |
|---|---|---|---|
| **P0** | R-14 | 标注文档适用范围；产出文档 ↔ 生产库对照表 | 产品 + 研发 |
| **P0** | R-16 | `product_usage_record` 归档/分区/告警 | DBA |
| **P0** | R-01, R-02, R-05 | 在目标环境验证并修复（若 U-19/U-20 确认未修复） | 研发 |
| **P1** | R-15, R-18 | dev 库治理；确认实例/账号隔离现状 | DBA + 安全 |
| **P1** | R-17 | 证实或否定跨库调用关系；绘制数据流图 | 研发 |
| **P1** | R-03, R-04, R-06, R-09 | 竞速/认证/模型列表/Schema 一致性问题 | 研发 |
| **P2** | R-07, R-08, R-10, R-11, R-19, R-20, R-22 | Key 管理、日志脱敏、测试表清理、PII 审查 | 研发 + 合规 |
| **P2** | R-21 | 明确 `new-api` 库定位 | 产品 |
| **P3** | R-12, R-13 | 部署文档整理；日志删除防护 | 运维 |

### 7.2 P0 验收标准（可验证）

**文档与数据：**

- [ ] 文档首部有「适用范围」（哪套代码、哪套库、哪套环境）  
- [ ] 已发布 ms-api-prod ↔ 文档表模型对照表  
- [ ] `product_usage_record` 已配置容量监控  
- [ ] U-01、U-05、U-12 至少三项有书面结论  

**API 行为（仅当 U-19/U-20 确认缺陷仍存在时适用）：**

- [ ] 不传 `pool_id` 时使用 Key 的 `default_pool_id`  
- [ ] 非法 `pool_id` 返回 403  
- [ ] OpenAI SDK 可调用 Chat、Models、Completions（路径一致）  

### 7.3 在待确认完成前，不宜对外陈述

- 「平台已具备文档中的 endpoints/pools 全套管理能力」（R-14/R-21）  
- 「API 数据仅存在于 ms-api-prod，不涉及其他业务库」（R-17/U-10 未证实）  
- 「日志不存储用户 Prompt」（R-08/U-05 未确认）  
- 「完全兼容 OpenAI SDK」（R-01）  

---

## 8. 附录

### 附录 A：文档表模型 vs 扫描结果对照

| 文档概念 | 文档表 | ms-api-prod | new-api |
|---|---|---|---|
| 上游端点 | `api_endpoints` | ❌ 不存在 | 未知（扫描无对应表名） |
| API 池 | `api_pools` | ❌ 不存在 | 未知 |
| API Key | `api_keys` | ⚠️ 可能对应 `product_trial` 等 | `tokens` 0 行 |
| 请求日志 | `request_logs` | ⚠️ 可能对应 **`product_usage_record`** | 未知 |
| 用户 | — | `user`（2,505 行） | `users`（1 行） |

> ⚠️ 表示名称/语义推测，需 U-06 确认。

### 附录 B：ms-api-prod Top 表（扫描）

| 表名 | 行数(估) | 大小(MB) | 占库比 | 注释 |
|---|---|---|---|---|
| product_usage_record | 115,972 | 90.61 | 89% | 产品使用统计表 |
| product_trial | 24,312 | 2.52 | 2.5% | 产品试用表 |
| order_detail | 10,277 | 1.52 | 1.5% | — |
| tracker_event | 8,251 | 1.52 | 1.5% | — |
| user | 2,505 | 0.47 | 0.5% | 用户信息表 |

### 附录 C：扫描报告架构关系（推断，待 U-10 证实）

```text
ms-api-prod (API网关)
    ├── ms_ai / ms_ai_dev (宠物AI问诊)
    ├── ms-fish / fish-recg-jd (智养鱼缸)
    └── ms-api-portal (开放平台门户)
```

来源：mysql_scan_report 第三节；**非代码证实。**

### 附录 D：参考文档

| 文档 | 用途 |
|---|---|
| [API_Documentation.md](./API_Documentation.md) | API 接口定义与文档自述缺陷 |
| [mysql_scan_report.md](./mysql_scan_report.md) | 2026-06-11 数据库扫描原始数据 |

### 附录 E：报告修订记录

| 版本 | 日期 | 修订说明 |
|---|---|---|
| v1.0 | 2026-06-11 | 首版 |
| v1.1 | 2026-06-11 | **仅保留 API 文档 + DB 扫描两源**；移除设计方案引用与前瞻性架构建议；待确认项保留 |

---

**报告结束**

*下一步：完成第 6 章待确认项 → 更新 v1.2 → 需求明确后再输出新的设计方案。*
