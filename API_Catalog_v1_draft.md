# 宠生万象 · API 能力目录（v1 草案）

> **状态**：产品理解草案，**待你搜集完整信息后替换/修正**  
> **用途**：目录页文案、对外文档骨架、商务一页纸、研发接口对齐参考  
> **关联**：[Pricing_Example_v1_draft.md](./Pricing_Example_v1_draft.md) · [Commercial_Platform_PRD_v1.md](./Commercial_Platform_PRD_v1.md)  

**图例：** 🟢 首版承诺 · 🟡 能力子项可能分波细化 · 🔴 草案假设，以最终算法能力为准  

---

## 总览

| 大类 | SKU | 调用方式 | 典型延迟 | 计费单位 |
|------|-----|----------|----------|----------|
| 视频行为 | V01 | 异步 | 秒～分钟级 | 分钟 |
| 图像识别 | I01 | 同步 | &lt; 3s | 次 |
| 声音识别 | A01 | 同步 | &lt; 5s | 次 |
| 健康测评 | H01 | 同步/异步 | 3～30s | 次（报告） |
| 通用对话 | T01 | 同步/流式 | 首 token &lt; 2s | 1k tokens |
| 问诊对话 | T02 | 同步/流式 | 首 token &lt; 2s | 1k tokens |

**统一说明：**

- 鉴权：`Authorization: Bearer <API_KEY>`  
- 仅当返回 **有效结果**（符合各 API schema）时扣费  
- 原始上传媒体默认 **72h** 删除；结构化结果默认保留 **180 天**  
- 所有健康/行为/对话输出均为 **AI 辅助信息，非兽医诊断**  

---

## 1. 视频行为分析（V01）

### 1.1 能力概述

对 **犬、猫** 上传短视频进行 AI 分析，识别 **可见行为与状态标签**（如玩耍、休息、异常动作、可能的应激表现等），输出结构化 JSON，供 App、摄像头、保险风控等场景使用。

### 1.2 典型场景

| 场景 | 客户类型 |
|------|----------|
| 宠物 App「拍视频看行为」 | App |
| 智能摄像头云端二次分析 | 硬件 |
| 寄养/保险行为佐证 | 保险 |
| 训练/丰容效果记录 | 平台、个人开发者 |

### 1.3 输入（草案）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `video_url` | string | ✅* | 可公网访问的视频 URL（HTTPS） |
| `video_file` | multipart | ✅* | 或通过「上传凭证」先传 OSS，再提交 URL |
| `pet_type` | enum | ✅ | `dog` / `cat` |
| `scene` | enum | 否 | `home` / `outdoor` / `cage` 等，影响标签权重 |
| `callback_url` | string | 否 | 任务完成 webhook |

\* `video_url` 与上传流程二选一。

**格式限制（草案）：**

- 格式：MP4、MOV  
- 时长：**3s～180s**（超出拒绝，不扣费）  
- 分辨率：建议 720p 及以上；最大文件 **100MB**  
- 帧率：≥ 15fps  

### 1.4 输出（有效结果 · 草案 schema）

```json
{
  "task_id": "tsk_xxx",
  "status": "completed",
  "pet_type": "dog",
  "duration_sec": 12.5,
  "behaviors": [
    { "label": "playing", "confidence": 0.87, "time_range": [2.1, 8.4] },
    { "label": "tail_wagging", "confidence": 0.72, "time_range": [3.0, 7.0] }
  ],
  "summary": "视频中犬只以玩耍行为为主，未见明显攻击或持续哀鸣。",
  "disclaimer": "本结果仅供参考，不能替代兽医诊断。"
}
```

**无效结果（不扣费）：** 视频无法解码、时长超限、未检测到宠物、算法内部错误。

### 1.5 调用流程

```text
POST /v1/video/behavior/tasks → task_id
GET  /v1/video/behavior/tasks/{id}  或  callback
成功且 behaviors 非空 → 扣费（按视频文件时长，见定价表）
```

### 1.6 限制与合规

- 首版 **犬/猫**；鱼、爬宠等 **不在 V01 范围**  
- 不得用于 **执法取证唯一依据**；合同需带免责声明  
- 含人脸/家居场景时，客户须自有隐私授权  

---

## 2. 图像识别（I01）

### 2.1 能力概述

对宠物相关 **单张图片** 进行识别，覆盖 **品种、表情情绪、皮肤异常、排泄物/呕吐物** 等子能力（🟡 子项可通过 `task_type` 指定，首版至少支持 2～3 个高频子项上线）。

### 2.2 子能力清单（草案）

| task_type | 说明 | 首版 |
|-----------|------|:----:|
| `breed` | 犬猫品种识别（含混种倾向） | 🟢 |
| `emotion` | 表情/情绪倾向（放松、紧张、疼痛疑似等） | 🟢 |
| `skin` | 皮肤/redis/脱毛等可见异常提示 | 🟡 |
| `stool_vomit` | 粪便/呕吐物性状粗分类（颜色、形态） | 🟡 |
| `general` | 通用宠物主体检测 + 场景描述 | 🟡 |

### 2.3 典型场景

| 场景 | 客户 |
|------|------|
| App 拍照识品种 | App、平台 |
| 电商商品图审核 | 平台 |
| 皮肤/便便自查引导 | App、保险初筛 |
| 医院预问诊素材分类 | 医院（辅助，非诊断） |

### 2.4 输入（草案）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `image_url` | string | ✅* | JPG/PNG/WebP |
| `image_base64` | string | ✅* | 与 url 二选一，建议 &lt; 4MB |
| `task_type` | enum | ✅ | 见上表 |
| `pet_type` | enum | 否 | `dog`/`cat`/`unknown`，提升准确率 |

**限制：** 最短边 ≥ 256px；最大 10MB；需包含清晰可见的宠物或目标物体。

### 2.5 输出（有效结果 · 示例：品种）

```json
{
  "task_type": "breed",
  "results": [
    { "breed": "Golden Retriever", "breed_cn": "金毛寻回犬", "confidence": 0.91 },
    { "breed": "Labrador Retriever", "breed_cn": "拉布拉多", "confidence": 0.06 }
  ],
  "pet_detected": true,
  "disclaimer": "本结果仅供参考，不能替代兽医诊断。"
}
```

**无效结果（不扣费）：** 无宠物主体、图片损坏、task_type 不支持、识别失败。

### 2.6 调用流程

```text
POST /v1/vision/analyze → 同步返回 JSON
成功且 pet_detected=true（或该 task 定义的有效字段存在）→ 扣费
```

---

## 3. 声音识别（A01）

### 3.1 能力概述

对 **短音频** 分析宠物相关声音，识别 **吠叫、呜咽、嚎叫、异常呼吸/咳嗽类** 等事件标签，适用于硬件麦克风、App 录音、监控声音告警。

### 3.2 子能力（草案）

| task_type | 说明 | 首版 |
|-----------|------|:----:|
| `bark` | 犬吠检测与分类（警告、玩耍、焦虑等倾向） | 🟢 |
| `whine_howl` | 呜咽、嚎叫 | 🟢 |
| `abnormal` | 异常声音（咳嗽、喘息等 **疑似**，非医学确诊） | 🟡 |
| `environment` | 环境音与宠物音分离 + 事件时间戳 | 🟡 |

### 3.3 输入（草案）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `audio_url` | string | ✅* | WAV/MP3/M4A |
| `audio_base64` | string | ✅* | 二选一 |
| `task_type` | enum | ✅ | |
| `pet_type` | enum | 否 | 默认 `dog` |

**限制：** 时长 **1s～60s**；采样率 ≥ 16kHz；最大 20MB。

### 3.4 输出（有效结果 · 示例）

```json
{
  "task_type": "bark",
  "events": [
    { "label": "bark_alert", "start_sec": 1.2, "end_sec": 1.8, "confidence": 0.84 }
  ],
  "summary": "检测到 2 次短促吠叫，倾向警戒。",
  "disclaimer": "本结果仅供参考，不能替代兽医诊断。"
}
```

**无效结果（不扣费）：** 无有效音频、时长超限、未检测到目标声音事件。

### 3.5 调用流程

```text
POST /v1/audio/analyze → 同步返回
成功且 events 数组非空（或该 task 有效字段满足）→ 扣费
```

---

## 4. 健康测评（H01）

### 4.1 能力概述

基于客户提交的 **结构化问卷答案 ± 可选图像/基础体征**，生成 **健康倾向测评报告**（非诊断），输出风险等级、关注项、养护建议。**独立 SKU**，不走旧 ms-ai 问诊库。

### 4.2 与 T02 问诊对话的区别

| 维度 | H01 健康测评 | T02 问诊对话 |
|------|-------------|-------------|
| 形态 | 结构化输入 → **固定报告** | 多轮自然语言 |
| 输出 | 报告 JSON/PDF 链接 | 对话文本 |
| 场景 | 保险初筛、年度体检助手、App 测评 | 即时问答、症状描述 |

### 4.3 典型场景

- 宠物 App「健康小测评」  
- 保险投保/续保健康问卷  
- 连锁机构会员健康管理（辅助）  

### 4.4 输入（草案）

| 字段 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `pet_profile` | object | ✅ | 物种、年龄、品种、绝育等 |
| `symptoms` | array | ✅ | 标准化症状码或描述 |
| `vitals` | object | 否 | 体重、体温等（若客户采集） |
| `attachments` | array | 否 | 图片 URL 列表（走 I01 能力融合） |
| `questionnaire_id` | string | 否 | 使用预设问卷模板 |

### 4.5 输出（有效结果 · 草案）

```json
{
  "report_id": "rpt_xxx",
  "risk_level": "medium",
  "focus_areas": ["digestive", "skin"],
  "summary": "根据您提供的信息，宠物存在中等程度的消化不适倾向，建议观察或咨询兽医。",
  "suggestions": ["清淡饮食 24h", "记录排便情况", "如呕吐持续请就医"],
  "disclaimer": "本报告为 AI 健康倾向评估，不是兽医诊断或处方。"
}
```

**无效结果（不扣费）：** 必填项缺失、问卷无法完成、报告生成失败。

### 4.6 调用流程

```text
POST /v1/health/assessment → 同步或异步（返回 report_id）
GET  /v1/health/assessment/{id}（若异步）
risk_level 等核心字段存在 → 扣费（按次/报告）
```

---

## 5. 文字 / 对话类

### 5.1 通用对话（T01）

**能力概述：** 宠物领域 **通用问答** — 养护知识、品种习性、训练建议、营养常识等，**不主动做症状问诊流程**。

**典型场景：** 客服机器人、百科问答、小程序助手。

**输入（草案）：**

| 字段 | 说明 |
|------|------|
| `messages` | OpenAI 风格 role/content 数组 |
| `pet_context` | 可选：宠物种类、年龄等 |
| `stream` | 是否 SSE 流式 |

**有效结果：** 返回完整 assistant 消息且 `finish_reason` 为 stop；按 **输入+输出总 tokens** 计费。

**限制：** 单次 context 建议 ≤ 8k tokens；禁止生成处方、确诊结论（系统 prompt 层约束 + 后置审核 🟡）。

---

### 5.2 问诊对话（T02）

**能力概述：** **多轮问诊式对话** — 引导用户描述症状、追问关键信息、给出 **就医建议级别** 的回复（观察/尽快就医/紧急），**非诊断、不开药**。

**典型场景：** App 智能问诊、医院预分诊助手、保险咨询。

**与旧 ms-ai：** 新平台 **新接口、新计费**；**不写 ms-ai 问诊库**；存量 ms-ai 客户不变。

**输入（草案）：** 同 T01，额外支持：

| 字段 | 说明 |
|------|------|
| `session_id` | 多轮会话 ID |
| `symptom_entry` | 可选：首轮症状结构化入口 |

**有效结果：** 每轮返回有效 assistant 回复；按 **tokens** 计费（刊例价高于 T01，见定价草案）。

**合规：** 每条回复带 `disclaimer`；高风险关键词触发「请立即就医」模板（🟡 规则待产品/法务完善）。

---

## 6. 目录页卡片文案（可直接贴官网 · 草案）

| 卡片 | 标题 | 一句话 |
|------|------|--------|
| V01 | 视频行为分析 | 上传犬猫短视频，获取行为标签与时间轴 |
| I01 | 图像识别 | 品种、情绪、皮肤、排泄物等一拍即识 |
| A01 | 声音识别 | 识别吠叫、呜咽与异常声音事件 |
| H01 | 健康测评 | 问卷 + 可选图片，生成健康倾向报告 |
| T01 | 通用对话 | 宠物养护与知识问答 |
| T02 | 问诊对话 | 多轮症状沟通与就医级别建议 |

---

## 7. 待你补充时建议带的字段

你搜集到完整信息后，可按下面 checklist 替换本文「草案」段落：

- [ ] 各子能力 **实际上线清单**（breed/skin/… 哪些真能做）  
- [ ] 准确 **输入输出 schema**（与算法/后端对齐）  
- [ ] **准确率/性能** 指标（对外是否宣传）  
- [ ] 犬猫以外 **宠种扩展计划**  
- [ ] 各 API **错误码表**  
- [ ] 问诊 **禁用词与升级就医规则**  

---

**版本**：API Catalog v1 草案 · 2026-06-10  
**品牌**：宠生万象  
**状态**：待产品/算法确认后升 v1.1  
