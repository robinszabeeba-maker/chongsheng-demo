services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: chongsheng
      POSTGRES_PASSWORD: chongsheng
      POSTGRES_DB: chongsheng
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
